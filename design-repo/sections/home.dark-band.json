{
  "id": "home.dark-band",
  "category": "content",
  "purpose": "Full-bleed dark closing-proof band; fixed content, no props. NOT content-parameterized: this component takes zero props in the real source -- its copy is single-instance and hardcoded, so the content contract below is empty/minimal by design, not an oversight.",
  "measuredFrom": "src/pages/HomePage.jsx:348-377 (DarkBand)",
  "content": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "constraints": {
    "onePerPage": true
  },
  "motion": {
    "pattern": "fadeUpOnScroll",
    "reducedMotionFallback": "static-no-animation"
  },
  "assetRoles": []
}
