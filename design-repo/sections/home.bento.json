{
  "id": "home.bento",
  "category": "feature",
  "purpose": "Bento-grid feature showcase; component takes zero props, each tile hardcoded via child BentoCopy. NOT content-parameterized: this component takes zero props in the real source -- its copy is single-instance and hardcoded, so the content contract below is empty/minimal by design, not an oversight.",
  "measuredFrom": "src/pages/HomePage.jsx:233-278 (Bento)",
  "content": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "constraints": {
    "onePerPage": true
  },
  "motion": {
    "pattern": "fadeUpOnScroll",
    "reducedMotionFallback": "static-no-animation"
  },
  "assetRoles": []
}
