{
  "id": "home.cta-band",
  "category": "conversion",
  "purpose": "Home page's own closing CTA band; fixed content, no props. NOT content-parameterized: this component takes zero props in the real source -- its copy is single-instance and hardcoded, so the content contract below is empty/minimal by design, not an oversight.",
  "measuredFrom": "src/pages/HomePage.jsx:378-430 (CtaBand)",
  "content": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "constraints": {
    "onePerPage": true,
    "mustBeLast": true
  },
  "motion": {
    "pattern": "fadeUpOnScroll",
    "reducedMotionFallback": "static-no-animation"
  },
  "assetRoles": []
}
